import React from 'react';
import { View, Text } from 'react-native';

const ProfileScreen = () => {
    return (
      <View>
        <Text>This should be the profile page.</Text>
      </View>
    );
  }

export default ProfileScreen