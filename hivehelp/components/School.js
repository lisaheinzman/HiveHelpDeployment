import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';
import guides from '.guides.json'

const schoolGuides = () => {

    const [guidesJson, setGuidesJson] = useState(null)

    useEffect(() => {
        
    })
    return(
        <View>
            <Text>{}</Text>
        </View>
    )
}